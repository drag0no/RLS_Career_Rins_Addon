-- Discovery shim for a new career module. The overhaul override manager
-- supplies the implementation under the same extension identity.
return require('lua.ge.extensions.overrides.career.modules.delivery.materialContractManager')
